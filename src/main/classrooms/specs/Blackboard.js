class Blackboard {

    returnIfCountsWith(classroom) {
        return classroom.returnIfCountsWithBlackboard();
    }

    has(classroomIfHas, classroomIfDoesNotHave) {
        return classroomIfHas;
    }

}

export default Blackboard;